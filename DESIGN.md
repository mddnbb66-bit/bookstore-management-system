# 网上书店管理系统 DESIGN

本项目参考 Shopify 的双轨设计语言，并结合课设展示场景做定制化落地：

- 前台采用清爽电商书店风格，使用浅色画布、薄荷绿点缀、圆角胶囊按钮、宽松留白。
- 后台采用浅色管理仪表盘风格，使用米白画布、浅灰边框、卡片统计区、左侧菜单与顶部栏。
- 不使用 Element Plus、Ant Design 和现成后台模板，全部组件以 Vue + 自定义 CSS 编写。

## 设计令牌

```css
:root {
  --bg-dark: #0b0b0b;
  --bg-light: #ffffff;
  --bg-cream: #fbfbf5;
  --bg-mint: #c1fbd4;
  --bg-pistachio: #d4f9e0;
  --text-main: #111111;
  --text-muted: #6b7280;
  --border: #e4e4e7;
  --shadow-soft: 0 8px 30px rgba(15, 23, 42, 0.08);
  --shadow-card: 0 2px 6px rgba(15, 23, 42, 0.06), 0 12px 28px rgba(15, 23, 42, 0.06);
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-pill: 999px;
}
```

## 前台设计

- 顶部导航使用白底细边框，配合深色品牌文字与胶囊按钮。
- 首页 Hero 使用大标题、深色图片区和分类标签，突出“校园课设可演示”的书店氛围。
- 图书卡片展示封面、分类、价格、折扣、库存、加入购物车按钮。
- 列表页支持搜索与分类筛选，方便截图演示。

## 后台设计

- 使用左侧菜单 + 顶部信息栏的经典管理结构。
- Dashboard 使用统计卡片、订单列表、预警区块。
- 表格、表单、弹窗、标签全部使用自定义样式组件。
- 统计页以 ECharts 折线图、柱状图、饼图配合低库存表格展示。

## 交互原则

- 所有主按钮统一胶囊形态。
- 主要操作使用黑底白字，次级操作使用白底黑边。
- 表单、表格、卡片统一浅边框和柔和阴影。
- 页面布局优先兼容 1920x1080 截图，同时在 768px 以下自适应。
